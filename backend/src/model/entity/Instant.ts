export type Instant = {
  id: string;
  serial: string;
  topic: string;
  cost: number;
  total: number;
  releasedAt: string;
  closedAt: string;
  createdAt: string | null;
  updatedAt: string | null;
};
